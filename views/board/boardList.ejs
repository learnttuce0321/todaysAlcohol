<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="stylesheet" href="\board\boardList.css" />
        <script src="https://use.fontawesome.com/releases/v6.2.0/js/all.js"></script>
        <link
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css"
            rel="stylesheet"
        />
        <link rel="stylesheet" href="/staticComponent/header2.css">
        <link rel="stylesheet" href="/staticComponent/footer.css">
        <title>Document</title>
    </head>
    <body>
        <header>
            <div id='header'></div>
        </header>
        <section class="container">
            <div class="main_container">
                <div class="title">
                    <p>게시판 별 게시글</p>
                    <p>게시글 클릭시 해당 게시판으로 이동합니다.</p>
                </div>
                <table
                    border="1"
                    cellspacing="0"
                >
                    <thead>
                        <tr class="menu">
                            <th>번호</th>
                            <th>제목</th>
                            <th>글쓴이</th>
                            <th>작성일</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for (let i =0; i < posts.length; i++) { %>
                        <tr id="tr_<%= posts[i].id %>">
                            <td><%= posts[i].id %></td>

                            <td>
                                <a href="/community/detail/<%= posts[i].id %>">
                                    <%= posts[i].title %>
                                </a>
                            </td>
                            <td><%= posts[i].userId %></td>
                            <td><%= posts[i].createdAt %></td>
                        </tr>
                        <% } %>
                    </tbody>
                </table>

                <!-- 페이징 -->
                <nav aria-label="Page navigation example">
                    <ul class="pagination">
                        <!-- 이전 -->
                        <li class="page-item">
                            <% if (page > 1) { %>
                            <a
                                class="page-link"
                                href="?page=<%= page - 1 %>"
                                aria-label="Previous"
                            >
                                <span aria-hidden="true">&laquo;</span>
                            </a>
                            <% } %>
                        </li>
                        <% for (let i = 1; i <= totalPages; i++) { %>
                        <li class="page-item">
                            <a
                                href="?page=<%= i %>"
                                class="<%= i === page ? 'active' : '' %> page-link"
                                ><%= i %></a
                            >
                        </li>
                        <% } %>

                        <li class="page-item">
                            <% if (page < totalPages) { %>
                            <a
                                class="page-link"
                                href="?page=<%= page + 1 %>"
                                aria-label="Next"
                            >
                                <span aria-hidden="true">&raquo;</span>
                            </a>
                            <% } %>
                        </li>
                    </ul>
                </nav>
                <!-- Pagination
            <div class="pagination">
                <% if (page > 1) { %>
                <a href="?page=<%= page - 1 %>">Previous</a>
                <% } %> <% for (let i = 1; i <= totalPages; i++) { %>
                <a
                    href="?page=<%= i %>"
                    class="<%= i === page ? 'active' : '' %>"
                    ><%= i %></a
                >
                <% } %> <% if (page < totalPages) { %>
                <a href="?page=<%= page + 1 %>">Next</a>
                <% } %>
            </div> -->
            </div>
        </section>
        <footer>
            <div id="footer"></div>
        </footer>
        <script src="\board\boardList.js"></script>
        <script src='/staticComponent/footer.js'></script>
        <script src="/staticComponent/header.js"></script>

    </body>
</html>
